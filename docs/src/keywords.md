# Keyword Reference

Functions for writing Abaqus `*Keyword` blocks. Each function returns a vector
of strings that can be concatenated and written to a `.inp` file.

```@autodocs
Modules = [AbaqusBuilder.Keyword]
```
